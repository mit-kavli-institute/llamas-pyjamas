#### This is a template configuration file to run the LLAMAS pipeline
#Its main purpose is to allow users to point to the files and directories that they wish to use for their data reduction
#The paths must be given as complete paths to ensure they can be found
#if files cannot be found or processed correctly then the pipeline defaults to the master calibration files

#==============================================================================
# RESUME / RE-RUN CONTROL
#==============================================================================
# By default the pipeline RESUMES: on a re-run it reuses any intermediate
# products already on disk and only does the work that is missing. Each stage
# checks for its own outputs and skips when they are present:
#   - flat pixel maps (flat/pixel_maps.fits + flat/flat_smooth_models.fits)
#   - per-science extraction + RSS generation (*_RSS_*.fits)
#   - fibre-to-fibre flat correction (*_FF.fits)
#   - sky-subtraction framework (*_FF_SKYSUB.fits)
#   - cube construction (*_cube_*.fits)
# So if a run fails late (e.g. only cube construction), re-running picks up from
# where it left off instead of repeating the whole reduction.
#
# Set clobber = true to force EVERY stage (including trace regeneration) to run
# from scratch, ignoring any existing products. Default: false.
# clobber = false

#==============================================================================
# BIAS CALIBRATION
#==============================================================================
# The pipeline requires two pre-combined master bias FITS files — one for each
# CCD readout mode. These must be created from your own raw bias exposures using
# the BiasLlamas class and placed in the Bias/ directory, OR supplied here as
# full paths. The correct file is selected automatically from the READ-MDE header.
#
# Default locations (pipeline looks here if no path is specified below):
#   llamas_pyjamas/Bias/slow_master_bias.fits
#   llamas_pyjamas/Bias/fast_master_bias.fits
#
# To use custom locations, uncomment and set the paths below:
#slow_bias_file = /path/to/your/slow_master_bias.fits
#fast_bias_file = /path/to/your/fast_master_bias.fits

#==============================================================================
# COSMIC RAY REMOVAL
#==============================================================================
# Enable L.A.Cosmic cosmic ray removal before spectral extraction (default: True)
# Set to False to skip cosmic ray removal (e.g. for calibration frames)
remove_cosmic_rays = True

# Optional: custom output directory for cosmic ray mask FITS files
# If not set, masks are saved to {extraction_output_dir}/masks/
#mask_output_dir = /path/to/custom/mask/output

#==============================================================================
# BIAS-FIRST PREPROCESSING (always on, not configurable)
#==============================================================================
# Bias subtraction is ALWAYS the first data operation applied to every frame
# (science, arc, flat, twilight, sky): the mode-appropriate (FAST/SLOW)
# 2D master bias plus the per-frame edge DC offset (see below) are removed
# up front and the corrected frames written to {output_dir}/bias_corrected/.
# All later stages (flat division, tracing, extraction) then operate on
# pedestal-free signal and skip their internal bias handling via the BIASSUB
# header stamp. There is no switch for this: dividing a flat into a frame
# that still carries its ~500 DN bias pedestal is unphysical and imprints
# the pixel flat's percent-level spectral features as tens-of-DN fake
# emission lines (spurious "sky lines", worst in the blue).

#==============================================================================
# EDGE BIAS (DC OFFSET) CORRECTION
#==============================================================================
# LLAMAS CCDs have no overscan region, and the per-channel bias DC level drifts
# through the night with temperature. The afternoon master bias removes the 2D
# fixed-pattern structure, but not this drift. This step measures a per-frame,
# per-extension DC offset from UNILLUMINATED pixels of each science/flat frame
# and subtracts it ON TOP of the master bias, levelling the red/green/blue
# channels. Applied to every frame that is extracted (science + flats).
#
# Tier 1 (default ON): the offset is measured over the top/bottom stripes that
# lie outside the fibre stack (pixels farther than edge_bias_min_distance from
# any fibre), using a robust sigma-clipped median. Placeholder (missing-camera)
# extensions are detected and skipped automatically.
#
# Master switch (default true). Set false to disable entirely.
edge_bias_dc_correction = true
# Distance in pixels from the nearest fibre for a pixel to count as an
# unilluminated top/bottom stripe pixel (default 20).
edge_bias_min_distance = 20
# Minimum stripe-mask pixels required; below this the correction is skipped for
# that extension (guards placeholders / odd geometry). Default 500.
edge_bias_min_pixels = 500
#
# Tier 2 (default OFF, experimental): additionally include the dark left/right
# dichroic-crossover / low-sensitivity zones. These vary per camera and per
# fibre (valid-x is a function of y), so they are taken from a flat-derived,
# per-pixel mask (flat/unillum_mask.fits, written during flat processing):
# on-fibre pixels whose flat throughput is below edge_bias_flat_frac of that
# fibre's own peak. Only meaningfully affects green/blue (red flats fill the
# full detector). Turn on to A/B-test against Tier 1.
#   NOTE: the flat mask is generated during flat processing, so enabling this on
#   an already-reduced dataset requires re-running the flat stage (clobber = true).
edge_bias_use_flat_mask = false
# Flat-throughput fraction (of each fibre's peak) below which a pixel is treated
# as unilluminated when building the flat mask (default 0.1).
edge_bias_flat_frac = 0.1
#
# QA: every run writes {extraction_output_dir}/edge_bias_levels.csv with, per
# extension, the subtracted offset (edge_dc) and BOTH candidate levels
# (stripes-only and stripes+flat) plus WTH-TEMP and timestamp — so the nightly
# drift and the two mask choices can be compared. Per-extension header keywords:
# EDGEDC, EDGENPIX, EDGEDMIN, EDGESRC.

#calibration files to include
red_flat_file = /Users/slh/Library/CloudStorage/Box-Box/slhughes/testing_files/trace_flats/LLAMAS_2025-05-23T12_34_47.516_mef.fits
green_flat_file = /Users/slh/Library/CloudStorage/Box-Box/slhughes/testing_files/trace_flats/LLAMAS_2025-05-23T12_27_47.585_mef.fits
blue_flat_file = /Users/slh/Library/CloudStorage/Box-Box/slhughes/testing_files/trace_flats/LLAMAS_2025-05-23T12_27_47.585_mef.fits

####NEW: added for fibre to fibre flat fielding routine
#If missing, pipeline will default to using the lamp flat files i.e. red_flat_file, green_flat_file, blue_flat_file
twilight_flat =  /Users/slh/Library/CloudStorage/Box-Box/slhughes/Llamas_Commissioning_Data/ut20260106/LLAMAS_2026-01-06_23-49-00.7_CAL22_mef.fits

# Optional per-colour twilight flats. These OVERRIDE the singular twilight_flat
# above for their colour (useful when blue twilight is a separate exposure).
# Any colour left unset falls back to the singular twilight_flat, then lamp-only.
#red_twilight_flat = /path/to/red_twilight_mef.fits
#green_twilight_flat = /path/to/green_twilight_mef.fits
#blue_twilight_flat = /path/to/blue_twilight_mef.fits


#science files to reduce
science_files = /Users/slh/Library/CloudStorage/Box-Box/slhughes/testing_files/LLAMAS_2025-05-23T00_51_25.019_mef.fits

#example of a list of science files to batch process
#science_files = path/to/file1.fits, path/to/file2.fits, path/to/file3.fits

#optional output directories for the trace generation and extraction files
#If these directories do not already exist they will be created at the time of processing

trace_output_dir = /Users/slh/Documents/Projects/Magellan_dev/LLAMAS/testing/fresh_test/traces
extraction_output_dir = /Users/slh/Documents/Projects/Magellan_dev/LLAMAS/testing/fresh_test/extractions
cube_output_dir = /Users/slh/Documents/Projects/Magellan_dev/LLAMAS/testing/fresh_test/cubes

# Ray parallelization control (optional, defaults to all CPU cores)
ray_num_cpus = 8
# Ray object store size in MB (optional, default 8192). Auto-clamped to <=30% of
# physical RAM so the object store can't starve Ray's logical memory pool.
# ray_object_store_mb = 8192
# Per-task memory reservation for extraction in MB (optional, default 0 = none).
# 0 schedules extraction purely on ray_num_cpus. Set >0 ONLY to throttle
# concurrency on low-RAM machines; a value larger than the available memory pool
# will make tasks unschedulable, so keep it small (e.g. 256).
# ray_task_memory_mb = 0

# --- Scratch / temp management (optional) -----------------------------------
# Ray writes session logs, object-store spill, and its package uploads into a
# temp dir. By default the pipeline redirects all of that into /tmp/llamas_ray/<pid>
# and always removes it on exit (even on failure), so /tmp no longer fills up.
#
# ray_temp_dir: base dir for Ray's temp output. MUST be a SHORT path (<= ~32 chars
#   on macOS / ~36 on Linux) because Ray binds AF_UNIX sockets under it; longer
#   paths are auto-rejected back to /tmp/llamas_ray with a warning. Point this at a
#   short path on a LARGE volume if your /tmp is small. Default: /tmp/llamas_ray.
# ray_temp_dir = /tmp/llamas_ray
#
# scratch_dir: alternate base for the temp dir if ray_temp_dir is unset (same
#   socket-length rule applies). Default: falls back to ray_temp_dir.
# scratch_dir = /scratch/llrs
#
# cleanup_scratch: remove the run's Ray scratch dir on exit (default: true). Set
#   false to keep it for debugging (Ray is still shut down cleanly).
# cleanup_scratch = true
#
# prune_ray_sessions: at startup also sweep generic /tmp/ray/session_* left by
#   crashed prior runs (default: false; may affect other Ray apps on the machine).
# prune_ray_sessions = false
#
# --- Pre-flight disk-space gate (optional) ----------------------------------
# Before any work, the pipeline checks free space on the temp and output volumes.
# It ABORTS immediately if space is guaranteed insufficient, and WARNS if it is
# tight. Tune or disable here.
# min_free_gb: hard floor of free space required to start, per volume (default 5).
# min_free_gb = 5
# disk_estimate_factor: multiplier on total input size used to estimate the output
#   footprint for the warning tier (default 5). Raise if your cubes are large.
# disk_estimate_factor = 5
# skip_disk_check: set true to bypass the pre-flight gate entirely (default false).
# skip_disk_check = false
#
# input_timeout_s: per-run budget for probing that every input file responds before
#   starting. If a file's filesystem is unresponsive (an offline cloud mount such as
#   Box/iCloud/Dropbox, or a dead network share), the pipeline aborts with a clear
#   message in this many seconds instead of hanging forever in stat (default 15).
# input_timeout_s = 15
#
# log_retention: keep only the newest N llamas_pipeline_*.log files (default 10).
# log_retention = 10
#
# To reclaim scratch left by OLDER runs (before this fix, or after a hard kill), run:
#   python -m llamas_pyjamas.Utils.clean_ray_scratch           # preview (dry run)
#   python -m llamas_pyjamas.Utils.clean_ray_scratch --yes     # actually delete

#==============================================================================
# ARC XSHIFT REFINEMENT
#==============================================================================
# Refine the per-fibre xshift (wavelength-alignment map) beyond the default
# cross-correlation quadratic. Off by default.
# refine_arc = false
# Channels to refine (default all): e.g. red,green
# refine_arc_channels = red,green,blue
#
# Method (when refine_arc = true):
#   perfiber (default) - existing refineArcX: independent per-fibre cubic on
#                        ~15-26 identified catalog lines. Data-starved; known
#                        to degrade edge consistency.
#   2d                 - hybrid model (Arc/arcSurface.py): robust 2D Legendre
#                        surface xshift(x, fibre) per camera fit to the FULL
#                        peak catalog pooled over all fibres, plus a small
#                        shrinkage-regularized per-fibre perturbation. Designed
#                        to fix the edge curvature mis-tracking that drives
#                        sky-subtraction residuals.
# refine_arc_method = 2d
#
# Night-arc line source (2d method): when {red,green,blue}_arc_file point to
# same-night arc exposures, the 2d method extracts them (into {output_dir}/arcs,
# resume-aware) and measures line centroids on THOSE spectra, so the solution
# aligns to the observation epoch instead of the packaged reference arc's
# (per-camera epoch offsets up to ~3 px were measured on may26 data). The
# packaged arc still provides line IDs and the xshift->wavelength map.
# refine_arc_use_night_arcs = true
#
# 2d-method tuning (defaults shown; all optional):
# arc_surface_order_x = 3          # Legendre order along dispersion (x)
# arc_surface_order_fiber = 2      # Legendre order across fibres
# arc_perturb_order = 0            # 0 = per-fibre offset, 1 = offset + tilt
# arc_perturb_min_lines = 8        # fewer matched lines => surface only
# arc_perturb_shrink_lines = 5     # shrinkage prior (equivalent lines)
# arc_use_unidentified_peaks = true  # use full catalog for the surface
# arc_catalog_min_sep = 8          # px; unidentified peaks with a closer
#                                  # catalog neighbour are dropped as blends
#                                  # (identified lines always kept)

#==============================================================================
# WAVELENGTH / XSHIFT QA
#==============================================================================
# Per-run QA of the wavelength calibration: xshift curvature structure per
# camera, arc-line fit residuals (when refine_arc runs), and post-sky-subtraction
# residuals at OH sky lines. Writes ONE self-contained HTML report (all figures
# embedded) plus a machine-readable CSV scorecard per science frame, so runs can
# be diffed against a baseline when the calibration algorithms change.
#
# Enable/disable (default: true). QA failures never abort a reduction.
wavelength_qa = true
# Output directory for QA reports (default: {output_dir}/QA).
# qa_output_dir = /path/to/QA
#
# QA can also be run standalone on existing products, without a pipeline run:
#   python -m llamas_pyjamas.QA.waveQA <..._sky1d_extractions.pkl> \
#       --qa-dir /path/to/QA --label baseline
# (add --pngs to also write individual PNG files)

#==============================================================================
# SKY SUBTRACTION FRAMEWORK
#==============================================================================
# Base sky model (skyModel_1d): populates the SKY extension that the framework
# refines. Leave True so the framework has a base model to build on.
sky_subtract = True

# Which fibres/regions build the sky model. This drives BOTH the base model
# (skyModel_1d) and, for consistency, the advanced framework's PCA basis below.
#   dimmest       (default) the sky_n_fibres faintest fibres per camera.
#   middle-third  legacy: the central third of fibres ranked by brightness.
#   skymap        fibres falling in a user sky region -> set sky_map_file.
#   frame         build from dedicated blank-sky exposures -> set sky_frame_files.
sky_selection_method = dimmest
# Number of faint fibres for the 'dimmest' method (default 20).
# sky_n_fibres = 20
# For sky_selection_method = skymap: a 2-D FITS marking sky regions. Either a
# mask (sky = nonzero pixels) or a flux image / non-sky-subtracted cube the
# pipeline thresholds. A celestial WCS is used when present (recommended).
# sky_map_file = /path/to/sky_map.fits
# For sky_selection_method = frame: dedicated blank-sky MEF(s) from your run.
# They are extracted through the normal path; all good fibres build the model.
# (If several are listed, only the first is used for now.)
# sky_frame_files = /path/to/sky1_mef.fits, /path/to/sky2_mef.fits

# Advanced sky-subtraction framework (post fibre-flat, per-colour). When True it
# reads the *_FF.fits files, applies source masking -> per-fibre OH scaling ->
# PCA/ZAP residual cleaning, and writes *_FF_SKYSUB.fits (consumed by the
# generate_skysub_cubes stage below). Default off.
sky_framework = False
# sky_method = pca            # 'pca' (full) or 'scaled' (OH scaling only)
# sky_pca_ncomp = 20          # PCA residual components removed
# sky_mask_method = whitelight
# sky_qa_plots = False        # write *_skyQA.png before/after OH-residual diagnostics

#==============================================================================
# CUBE CONSTRUCTION PARAMETERS
#==============================================================================
# Enable/disable cube generation (set to False if only 2D RSS products needed)
generate_cubes = True

# Two independent cube sets are built by default (both require generate_cubes = True):
#   generate_standard_cubes -> cubes from the fibre-flat RSS  -> ..._cube_{color}.fits
#   generate_skysub_cubes   -> cubes from the sky-framework RSS -> ..._cube_{color}_skysub.fits
# Set either to False to suppress that set. The _skysub cubes only appear when the
# sky framework ran (sky_framework = True) and produced *_FF_SKYSUB.fits files.
generate_standard_cubes = True
generate_skysub_cubes = True

# Cube construction method: simple (default, MUSE-like), crr, or traditional
# simple = Fast, MUSE-inspired interpolation (recommended)
# crr = Covariance-Regularized Reconstruction (slower, experimental)
# traditional = Legacy method
cube_method = simple

# Simple cube constructor parameters (used when cube_method = simple)
cube_pixel_size = 0.3          # Spatial pixel size in arcseconds (default: 0.3)
cube_fiber_pitch = 0.75        # Fiber-to-fiber pitch in arcseconds (default: 0.75)
cube_wave_sampling = 1.0       # Wavelength sampling factor (1.0 = native, 0.5 = 2x oversample)
cube_radius = 1.5              # Interpolation radius in arcseconds (default: 1.5, ~2 fiber pitches)
cube_min_weight = 0.01         # Minimum weight threshold for interpolation (default: 0.01)

# Legacy parameters for CRR and traditional methods (keep for backward compatibility)
CRR_cube = False               # Use CRR method (overrides cube_method if True)
CRR_parallel = False           # Enable parallel processing for CRR method

