# Documentation build dependencies.
# Install with: pip install -r docs/requirements.txt
#
# Heavy / environment-specific pipeline dependencies (ray, pypeit, cloudpickle,
# PyQt6, lacosmic, pyds9) are NOT listed here: they are mocked at build time via
# autodoc_mock_imports in conf.py, so they do not need to be installed to build
# the docs. Only the lightweight imports that autodoc actually executes are needed.

# Core Sphinx + theme
sphinx>=7,<9
furo>=2024.1.29
sphinx-autodoc-typehints>=2.0

# Runtime imports that autodoc executes (not mocked)
numpy
scipy
astropy
matplotlib
h5py
psutil
pyyaml
